import React from "react";

const Loader = () => {
  return<div className="h-full w-full bg-[#18181867] flex justify-center items-center fixed top-0 left-0 z-50">
    <span className="loader"></span>
  </div>
};

export default Loader;
